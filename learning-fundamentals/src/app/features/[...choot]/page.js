
function featuresSummary({params}) {

  return (
    <div className="h-[75vh]">
        <h1>slug {params.slug[0]}</h1>
        {console.log("hello")}
    </div>
  );
}

export default featuresSummary;
